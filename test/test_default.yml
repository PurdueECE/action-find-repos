name: Default Test
on: push

jobs:
  gather-repos:
    runs-on: ubuntu-latest
    steps:
      - uses: ./
        id: get_repos
        with:
          org: PurdueECE364
          pat: secret
          pattern: ^prelabs-*$
      - run: "echo results: ${{steps.get_repos.outputs.repos}}"